{
  "type": "tabs",
  "tabs": [
    {
      "label": "Production",
      "blocks": [
        {
          "type": "code",
          "language": "yaml",
          "filename": "docker-compose.yml",
          "description": "Production compose. No volume mounts -- the container runs its own copy of the code.",
          "code": "version: \"3.9\"\n\nservices:\n  frontend:\n    build:\n      context: ./frontend\n    ports:\n      - \"3000:3000\"\n    depends_on:\n      - backend\n\n  backend:\n    build:\n      context: ./backend\n    ports:\n      - \"5000:5000\"\n    environment:\n      - FLASK_ENV=production\n      - CORS_ORIGINS=http://localhost:3000"
        }
      ]
    },
    {
      "label": "Development",
      "blocks": [
        {
          "type": "code",
          "language": "yaml",
          "filename": "docker-compose.dev.yml",
          "description": "Dev compose with volume mounts. Changes to source files on the host are immediately visible inside the containers. The /app/node_modules anonymous volume prevents the host node_modules from overwriting the container's.",
          "code": "version: \"3.9\"\n\nservices:\n  frontend:\n    build:\n      context: ./frontend\n    ports:\n      - \"3000:3000\"\n    volumes:\n      - ./frontend:/app\n      - /app/node_modules\n    depends_on:\n      - backend\n\n  backend:\n    build:\n      context: ./backend\n    ports:\n      - \"5000:5000\"\n    environment:\n      - FLASK_ENV=development\n      - CORS_ORIGINS=http://localhost:3000\n    volumes:\n      - ./backend:/app"
        }
      ]
    }
  ]
}
