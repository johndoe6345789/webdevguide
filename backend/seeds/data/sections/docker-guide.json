{
  "title": "Docker Guide",
  "slug": "docker-guide",
  "description": "Learn how to package your applications into containers that run the same everywhere. No more \"works on my machine\" problems.",
  "icon": "Inventory2",
  "order": 10,
  "estimatedMinutes": 45,
  "difficulty": "intermediate",
  "blocks": [
    {
      "type": "text",
      "heading": "What is Docker?",
      "headingLevel": 2,
      "body": "Before we look at any code, let us understand the problem Docker solves and why every developer should know it."
    },
    {
      "type": "text",
      "body": "Imagine you are moving to a new apartment. You could carry everything loose -- plates, clothes, books -- and hope nothing breaks along the way. Or you could put everything into **shipping containers**. Each container is sealed, standardized, and works the same whether it goes on a truck, a train, or a cargo ship."
    },
    {
      "type": "text",
      "body": "Docker does the same thing for software. It takes your application, all its dependencies (libraries, tools, configuration files), and everything it needs to run, then packages it all into a sealed **container**. That container will run exactly the same way on your laptop, your teammate's laptop, and a server in the cloud."
    },
    {
      "type": "alert",
      "severity": "warning",
      "title": "The \"works on my machine\" problem",
      "body": "You write code, it runs perfectly on your computer, but when you share it with a teammate it crashes. Maybe they have a different version of Python. Maybe they are missing a library. Maybe their operating system works differently. Docker eliminates this problem entirely because everyone runs the same container."
    },
    {
      "type": "cardGrid",
      "columns": 3,
      "cards": [
        {
          "title": "Consistency",
          "body": "Your app runs the same everywhere -- your machine, your coworker's machine, and the production server. No more environment surprises.",
          "icon": "ViewInAr"
        },
        {
          "title": "Isolation",
          "body": "Each container is its own little world. One project can use Python 3.9 while another uses Python 3.12, and they will never conflict with each other.",
          "icon": "Layers"
        },
        {
          "title": "Speed",
          "body": "Containers start in seconds, not minutes. Setting up a new developer on your project goes from hours of installation to a single command.",
          "icon": "RocketLaunch"
        }
      ]
    },
    {
      "type": "text",
      "heading": "Docker vs Virtual Machines",
      "headingLevel": 2,
      "body": "You might have heard of virtual machines (VMs). Containers are a different approach to the same idea."
    },
    {
      "type": "text",
      "body": "A **virtual machine** is like renting an entire apartment in a building. You get your own kitchen, bathroom, living room -- a complete, self-contained unit. It works great, but it takes a lot of space and resources."
    },
    {
      "type": "text",
      "body": "A **container** is more like renting a room in a shared house. You have your own private room (your app and dependencies), but you share the kitchen and bathroom (the operating system kernel) with other tenants. This is far more efficient."
    },
    {
      "type": "table",
      "heading": "Docker vs VM Comparison",
      "headers": [
        "Feature",
        "Docker Container",
        "Virtual Machine"
      ],
      "rows": [
        [
          "Startup time",
          "Seconds",
          "Minutes"
        ],
        [
          "Size",
          "Megabytes (typically 50-500 MB)",
          "Gigabytes (often 2-10 GB)"
        ],
        [
          "OS",
          "Shares host OS kernel",
          "Runs its own full OS"
        ],
        [
          "Performance",
          "Near-native speed",
          "Some overhead from virtualization"
        ],
        [
          "Isolation",
          "Process-level isolation",
          "Full hardware-level isolation"
        ],
        [
          "Resource usage",
          "Lightweight -- run dozens on one machine",
          "Heavy -- each VM reserves its own RAM and CPU"
        ]
      ]
    },
    {
      "type": "alert",
      "severity": "info",
      "title": "When to use a VM instead",
      "body": "Virtual machines are better when you need complete security isolation (running untrusted code) or when you need to run a completely different operating system (like running Windows on a Mac). For most web development work, containers are the right choice."
    },
    {
      "type": "text",
      "heading": "Key Docker Concepts",
      "headingLevel": 2,
      "body": "Five terms you will see everywhere. Learn these and everything else will click."
    },
    {
      "type": "cardGrid",
      "columns": 2,
      "cards": [
        {
          "title": "Image",
          "body": "An image is a read-only template that contains everything needed to run your app -- the code, runtime, libraries, and configuration. Think of it like a cookie cutter. You create one image, and you can stamp out as many identical containers as you want.",
          "chip": "A recipe or blueprint"
        },
        {
          "title": "Container",
          "body": "A container is what you get when you \"run\" an image. It is a live, running process with its own isolated filesystem and network. If an image is a cookie cutter, a container is an actual cookie. You can run multiple containers from the same image.",
          "chip": "A running instance of an image"
        },
        {
          "title": "Dockerfile",
          "body": "A Dockerfile is a plain text file with step-by-step instructions that tell Docker how to build an image. It says things like \"start from Python 3.12, copy my code, install these packages, and run this command.\"",
          "chip": "The instructions to build an image"
        },
        {
          "title": "Volume",
          "body": "By default, when a container is deleted, all its data is lost. A volume is like an external hard drive you plug into a container. Data stored in a volume survives even when the container is removed. Essential for databases and user uploads.",
          "chip": "Persistent storage"
        },
        {
          "title": "Port Mapping",
          "body": "Containers are isolated -- your computer cannot reach inside by default. Port mapping creates a tunnel. If your app runs on port 3000 inside the container, you can map it to port 3000 on your computer so you can open localhost:3000 in your browser.",
          "chip": "Connecting container ports to your computer"
        }
      ]
    },
    {
      "type": "alert",
      "severity": "success",
      "title": "How they all connect",
      "body": "You write a Dockerfile (instructions). Docker uses it to build an Image (blueprint). You run the image to create a Container (live app). You attach a Volume for data that should persist. You set up Port Mapping so you can access the container from your browser."
    },
    {
      "type": "text",
      "heading": "Installing Docker",
      "headingLevel": 2,
      "body": "Getting Docker on your machine takes just a few minutes."
    },
    {
      "type": "tabs",
      "tabs": [
        {
          "label": "Mac",
          "blocks": [
            {
              "type": "step",
              "step": 1,
              "title": "Download Docker Desktop",
              "body": "Go to docker.com/products/docker-desktop and download Docker Desktop for Mac. Choose the correct chip -- Apple Silicon for M1/M2/M3/M4 Macs, or Intel for older Macs."
            },
            {
              "type": "step",
              "step": 2,
              "title": "Install the app",
              "body": "Open the downloaded .dmg file and drag Docker into your Applications folder, just like any other Mac app."
            },
            {
              "type": "step",
              "step": 3,
              "title": "Launch and verify",
              "body": "Open Docker Desktop from your Applications folder. You will see the Docker whale icon in your menu bar. Open a terminal and run:",
              "code": "docker --version",
              "language": "bash"
            }
          ]
        },
        {
          "label": "Windows",
          "blocks": [
            {
              "type": "step",
              "step": 1,
              "title": "Check system requirements",
              "body": "You need Windows 10 version 2004 or higher (or Windows 11). Docker Desktop uses WSL 2. If you do not have WSL 2 yet, open PowerShell as Administrator and run:",
              "code": "wsl --install",
              "language": "powershell"
            },
            {
              "type": "step",
              "step": 2,
              "title": "Download Docker Desktop",
              "body": "Go to docker.com/products/docker-desktop and download Docker Desktop for Windows."
            },
            {
              "type": "step",
              "step": 3,
              "title": "Install and restart",
              "body": "Run the installer and follow the prompts. You may need to restart your computer. After restarting, Docker Desktop will launch automatically."
            },
            {
              "type": "step",
              "step": 4,
              "title": "Verify the installation",
              "body": "Open a terminal (Command Prompt, PowerShell, or Git Bash) and run:",
              "code": "docker --version",
              "language": "bash"
            }
          ]
        },
        {
          "label": "Linux (Ubuntu)",
          "blocks": [
            {
              "type": "step",
              "step": 1,
              "title": "Update your package index",
              "body": "Start by updating your package index.",
              "code": "sudo apt-get update",
              "language": "bash"
            },
            {
              "type": "step",
              "step": 2,
              "title": "Install prerequisite packages",
              "body": "Install the required prerequisite packages.",
              "code": "sudo apt-get install \\\n  ca-certificates \\\n  curl \\\n  gnupg \\\n  lsb-release",
              "language": "bash"
            },
            {
              "type": "step",
              "step": 3,
              "title": "Add Docker's official GPG key and repository",
              "body": "Add the GPG key and repository so your system can download Docker packages.",
              "code": "# Add Docker's official GPG key\nsudo install -m 0755 -d /etc/apt/keyrings\ncurl -fsSL https://download.docker.com/linux/ubuntu/gpg | \\\n  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg\n\n# Add the repository\necho \\\n  \"deb [arch=$(dpkg --print-architecture) \\\n  signed-by=/etc/apt/keyrings/docker.gpg] \\\n  https://download.docker.com/linux/ubuntu \\\n  $(lsb_release -cs) stable\" | \\\n  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null",
              "language": "bash"
            },
            {
              "type": "step",
              "step": 4,
              "title": "Install Docker Engine",
              "body": "Update the package index again and install Docker Engine.",
              "code": "sudo apt-get update\nsudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin",
              "language": "bash"
            },
            {
              "type": "step",
              "step": 5,
              "title": "Run Docker without sudo (optional but recommended)",
              "body": "Add your user to the docker group so you do not need sudo for every command.",
              "code": "sudo usermod -aG docker $USER\n# Log out and log back in for this to take effect",
              "language": "bash"
            },
            {
              "type": "step",
              "step": 6,
              "title": "Verify the installation",
              "body": "Check that Docker is installed correctly.",
              "code": "docker --version",
              "language": "bash"
            }
          ]
        }
      ]
    },
    {
      "type": "text",
      "heading": "Your First Dockerfile",
      "headingLevel": 2,
      "body": "A Dockerfile is a recipe that tells Docker how to build your image. Let us walk through a real one line by line."
    },
    {
      "type": "text",
      "body": "This example uses a **multi-stage build** -- a technique where you use one stage to build your app and a second, smaller stage to run it. This keeps your final image small and clean."
    },
    {
      "type": "code",
      "language": "dockerfile",
      "filename": "Dockerfile",
      "code": "# ---- Stage 1: Build stage ----\n# Start from a Python 3.12 image with the \"slim\" variant (smaller size)\nFROM python:3.12-slim AS builder\n\n# Set the working directory inside the container\nWORKDIR /app\n\n# Copy only the requirements file first (for better caching)\nCOPY requirements.txt .\n\n# Install Python dependencies\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Copy the rest of the application code\nCOPY . .\n\n# ---- Stage 2: Production stage ----\n# Start fresh from a clean slim image\nFROM python:3.12-slim\n\n# Set the working directory\nWORKDIR /app\n\n# Copy installed packages from the builder stage\nCOPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages\nCOPY --from=builder /usr/local/bin /usr/local/bin\n\n# Copy application code from the builder stage\nCOPY --from=builder /app .\n\n# Tell Docker this container will listen on port 5000\nEXPOSE 5000\n\n# The command to run when the container starts\nCMD [\"python\", \"app.py\"]"
    },
    {
      "type": "text",
      "heading": "What each instruction does",
      "headingLevel": 3,
      "body": ""
    },
    {
      "type": "cardGrid",
      "columns": 2,
      "cards": [
        {
          "title": "FROM",
          "body": "Sets the base image. Every Dockerfile starts with FROM. Think of it as choosing your starting ingredients. \"python:3.12-slim\" means \"start with a lightweight Linux system that already has Python 3.12 installed.\""
        },
        {
          "title": "WORKDIR",
          "body": "Sets the current directory inside the container. All following commands will run from this directory. It is like running \"cd /app\" -- if the folder does not exist, Docker creates it."
        },
        {
          "title": "COPY",
          "body": "Copies files from your computer into the container. \"COPY requirements.txt .\" means \"take the requirements.txt from my project folder and put it in the current working directory inside the container.\""
        },
        {
          "title": "RUN",
          "body": "Executes a command during the build process. This is where you install dependencies, compile code, or run setup scripts. Each RUN creates a new layer in the image."
        },
        {
          "title": "EXPOSE",
          "body": "Documents which port the app will use. This does not actually open the port -- it is like a note for other developers. You still need to use -p when running the container."
        },
        {
          "title": "CMD",
          "body": "The command that runs when the container starts. There can be only one CMD. In this case it runs \"python app.py\" to start the application."
        }
      ]
    },
    {
      "type": "alert",
      "severity": "info",
      "title": "Why copy requirements.txt separately?",
      "body": "Docker caches each step. If your code changes but requirements.txt does not, Docker will reuse the cached layer with the installed packages instead of reinstalling everything. This makes rebuilds much faster."
    },
    {
      "type": "text",
      "heading": "Multi-Stage Dockerfile for Node.js",
      "headingLevel": 3,
      "body": "The multi-stage pattern works for any language. Here is a production-ready Dockerfile for a Node.js application. The first stage installs dependencies and builds the project, the second stage copies only what is needed to run."
    },
    {
      "type": "code",
      "language": "dockerfile",
      "filename": "Dockerfile (Node.js)",
      "code": "# ---- Stage 1: Build ----\nFROM node:22-alpine AS builder\n\nWORKDIR /app\n\n# Copy package files first for better layer caching\nCOPY package.json package-lock.json ./\n\n# Install ALL dependencies (including devDependencies for the build)\nRUN npm ci\n\n# Copy source code and build\nCOPY . .\nRUN npm run build\n\n# ---- Stage 2: Production ----\nFROM node:22-alpine\n\nWORKDIR /app\n\n# Install only production dependencies\nCOPY package.json package-lock.json ./\nRUN npm ci --omit=dev\n\n# Copy the built output from the builder stage\nCOPY --from=builder /app/dist ./dist\n\n# Run as non-root user for security\nRUN addgroup -S appgroup && adduser -S appuser -G appgroup\nUSER appuser\n\nEXPOSE 3000\n\nCMD [\"node\", \"dist/index.js\"]"
    },
    {
      "type": "text",
      "heading": "Dockerfile for Python/Flask",
      "headingLevel": 3,
      "body": "Flask applications need a production WSGI server like Gunicorn. This Dockerfile sets that up along with a non-root user for security."
    },
    {
      "type": "code",
      "language": "dockerfile",
      "filename": "Dockerfile (Python/Flask)",
      "code": "FROM python:3.12-slim\n\n# Prevent Python from writing .pyc files and enable unbuffered output\nENV PYTHONDONTWRITEBYTECODE=1\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /app\n\n# Install system dependencies (if needed for packages like psycopg2)\nRUN apt-get update && \\\n    apt-get install -y --no-install-recommends gcc libpq-dev && \\\n    rm -rf /var/lib/apt/lists/*\n\n# Copy and install Python dependencies\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt && \\\n    pip install --no-cache-dir gunicorn\n\n# Copy application code\nCOPY . .\n\n# Create a non-root user\nRUN adduser --disabled-password --no-create-home appuser\nUSER appuser\n\nEXPOSE 5000\n\n# Use Gunicorn for production (4 workers, bind to all interfaces)\nCMD [\"gunicorn\", \"--bind\", \"0.0.0.0:5000\", \"--workers\", \"4\", \"app:create_app()\"]"
    },
    {
      "type": "text",
      "heading": "The .dockerignore File",
      "headingLevel": 3,
      "body": "When Docker builds an image, it sends your entire project directory to the Docker daemon as \"build context.\" Without a .dockerignore file, Docker copies everything -- including node_modules, .git history, environment files, and other junk you do not want in the image."
    },
    {
      "type": "alert",
      "severity": "info",
      "title": ".dockerignore is like .gitignore for Docker",
      "body": "Just as .gitignore tells Git which files to skip when committing, .dockerignore tells Docker which files to skip when building. It makes your builds faster (less data to send) and your images smaller (less junk copied in). Always create one at the root of any Docker project."
    },
    {
      "type": "code",
      "language": "text",
      "filename": ".dockerignore",
      "code": "# Version control\n.git\n.gitignore\n\n# Dependencies (will be installed inside the container)\nnode_modules\n__pycache__\n*.pyc\n.venv\n\n# Environment and secrets\n.env\n.env.*\n*.pem\n*.key\n\n# IDE and editor files\n.vscode\n.idea\n*.swp\n*.swo\n\n# OS files\n.DS_Store\nThumbs.db\n\n# Build artifacts and logs\ndist\nbuild\ncoverage\n*.log\n\n# Docker files (no need to copy Dockerfiles into the image)\nDockerfile*\ndocker-compose*\n.dockerignore\n\n# Documentation\nREADME.md\nLICENSE\ndocs/"
    },
    {
      "type": "alert",
      "severity": "error",
      "title": "Never put secrets in Dockerfiles",
      "body": "Docker images are made of layers, and every layer is stored permanently. If you put a password, API key, or private key in a RUN, COPY, or ENV instruction, anyone who pulls the image can extract it -- even if you delete the file in a later layer. Use environment variables at runtime (docker run -e or docker-compose environment) or Docker secrets instead."
    },
    {
      "type": "text",
      "heading": "Building and Running",
      "headingLevel": 2,
      "body": "Now that you have a Dockerfile, let us turn it into a running container."
    },
    {
      "type": "step",
      "step": 1,
      "title": "Build the image",
      "body": "Open a terminal in the folder that contains your Dockerfile and run:",
      "code": "docker build -t my-app .",
      "language": "bash"
    },
    {
      "type": "list",
      "heading": "Breaking this down:",
      "items": [
        "`docker build` -- tells Docker to build an image",
        "`-t my-app` -- gives the image a name (tag). You can choose any name.",
        "`.` -- the dot means \"use the current directory\" as the build context (where Docker looks for the Dockerfile and your code)"
      ]
    },
    {
      "type": "step",
      "step": 2,
      "title": "Run the container",
      "body": "Once the build finishes, start a container from your new image:",
      "code": "docker run -p 5000:5000 my-app",
      "language": "bash"
    },
    {
      "type": "list",
      "heading": "Breaking this down:",
      "items": [
        "`docker run` -- creates and starts a new container",
        "`-p 5000:5000` -- maps port 5000 on your computer to port 5000 inside the container. Format is HOST_PORT:CONTAINER_PORT.",
        "`my-app` -- the name of the image to run"
      ]
    },
    {
      "type": "text",
      "body": "Now open **http://localhost:5000** in your browser -- your app is running inside a container!"
    },
    {
      "type": "step",
      "step": 3,
      "title": "Run in the background (detached mode)",
      "body": "The previous command locks up your terminal. To run in the background, add the -d flag:",
      "code": "docker run -d -p 5000:5000 --name my-running-app my-app",
      "language": "bash"
    },
    {
      "type": "text",
      "body": "The `--name` flag gives your container a human-readable name so you can refer to it easily when stopping or checking logs."
    },
    {
      "type": "alert",
      "severity": "success",
      "title": "That is it!",
      "body": "Two commands -- `docker build` and `docker run` -- are all you need to go from code to a running app. Everything else in Docker builds on top of these two ideas."
    },
    {
      "type": "text",
      "heading": "Dockerize Your First App",
      "headingLevel": 2,
      "body": "Let us walk through dockerizing an existing Node.js Express application from start to finish. By the end of these steps you will have a working containerized app."
    },
    {
      "type": "step",
      "step": 1,
      "title": "Create a .dockerignore file",
      "body": "Before writing the Dockerfile, create a .dockerignore in your project root to prevent unnecessary files from being copied into the image. At a minimum, exclude node_modules, .git, .env, and any build artifacts.",
      "code": "node_modules\n.git\n.env\n.env.*\ncoverage\n.DS_Store",
      "language": "text"
    },
    {
      "type": "step",
      "step": 2,
      "title": "Write the Dockerfile",
      "body": "Create a file named `Dockerfile` (no extension) in the project root. Start from the official Node.js Alpine image, copy your package files, install dependencies, copy your source code, and define the start command.",
      "code": "FROM node:22-alpine\n\nWORKDIR /app\n\nCOPY package.json package-lock.json ./\nRUN npm ci --omit=dev\n\nCOPY . .\n\nEXPOSE 3000\nCMD [\"node\", \"index.js\"]",
      "language": "dockerfile"
    },
    {
      "type": "step",
      "step": 3,
      "title": "Build and run the image",
      "body": "Open a terminal in your project directory. Build the image, then run it with port mapping so you can access it from your browser.",
      "code": "# Build the image\ndocker build -t my-express-app .\n\n# Run the container (maps port 3000 on your machine to port 3000 in the container)\ndocker run -p 3000:3000 my-express-app",
      "language": "bash"
    },
    {
      "type": "step",
      "step": 4,
      "title": "Verify and iterate",
      "body": "Open http://localhost:3000 in your browser. If the app works, congratulations -- it is containerized! If it does not, check the container logs for errors. From here you can add a health check, switch to a multi-stage build for smaller images, or add Docker Compose for additional services like a database.",
      "code": "# Check logs if something is wrong\ndocker logs $(docker ps -q --filter ancestor=my-express-app)\n\n# Stop the container when done\ndocker stop $(docker ps -q --filter ancestor=my-express-app)",
      "language": "bash"
    },
    {
      "type": "text",
      "heading": "Docker Compose",
      "headingLevel": 2,
      "body": "Real projects usually have more than one container. Docker Compose lets you define and run them all together."
    },
    {
      "type": "text",
      "body": "Imagine your project has a React frontend, a Python backend, and a database. You could run three separate `docker run` commands, remembering all the flags for each one. Or you could describe everything in a single file and start it all with one command."
    },
    {
      "type": "text",
      "body": "That file is called **docker-compose.yml**. It uses YAML format (a simple, human-readable way to write configuration)."
    },
    {
      "type": "code",
      "language": "yaml",
      "filename": "docker-compose.yml",
      "code": "services:\n  # ---- Frontend (React/Next.js) ----\n  frontend:\n    build:\n      context: ./frontend          # Where to find the Dockerfile\n      dockerfile: Dockerfile       # Which Dockerfile to use\n    ports:\n      - \"3000:3000\"                # Map port 3000 on your machine to port 3000 in the container\n    depends_on:\n      backend:\n        condition: service_healthy  # Wait for backend to be healthy before starting\n    environment:\n      - API_URL=http://backend:5000 # Containers can talk to each other by service name\n\n  # ---- Backend (Python Flask) ----\n  backend:\n    build:\n      context: ./backend\n      dockerfile: Dockerfile\n    ports:\n      - \"5000:5000\"\n    volumes:\n      - app-data:/app/data         # Persist data even if container is recreated\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:5000/health\"]\n      interval: 30s                # Check every 30 seconds\n      timeout: 10s                 # Wait up to 10 seconds for a response\n      start_period: 10s            # Give the app 10 seconds to start up\n      retries: 3                   # Fail after 3 unsuccessful checks\n\n  # ---- Database (PostgreSQL) ----\n  db:\n    image: postgres:16             # Use the official PostgreSQL image (no build needed)\n    ports:\n      - \"5432:5432\"\n    environment:\n      POSTGRES_USER: myuser\n      POSTGRES_PASSWORD: mypassword\n      POSTGRES_DB: myapp\n    volumes:\n      - db-data:/var/lib/postgresql/data  # Persist database data\n\n# Named volumes — Docker manages the storage location for you\nvolumes:\n  app-data:\n  db-data:"
    },
    {
      "type": "text",
      "heading": "Docker Compose with Volumes and Networking",
      "headingLevel": 3,
      "body": "For more complex setups, you can define custom networks to isolate traffic between services. This example shows a microservices architecture where the frontend can only talk to the backend, and only the backend can talk to the database."
    },
    {
      "type": "code",
      "language": "yaml",
      "filename": "docker-compose.yml (advanced networking)",
      "code": "services:\n  frontend:\n    build: ./frontend\n    ports:\n      - \"3000:3000\"\n    networks:\n      - frontend-net             # Frontend can only reach the backend\n    depends_on:\n      - backend\n\n  backend:\n    build: ./backend\n    ports:\n      - \"5000:5000\"\n    networks:\n      - frontend-net             # Backend is reachable by frontend\n      - backend-net              # Backend can reach the database\n    volumes:\n      - uploads:/app/uploads     # Persist user uploads\n      - ./logs:/app/logs         # Bind mount: logs appear on host filesystem\n    environment:\n      DATABASE_URL: postgres://myuser:mypassword@db:5432/myapp\n\n  db:\n    image: postgres:16\n    networks:\n      - backend-net              # Database is only reachable by the backend\n    volumes:\n      - db-data:/var/lib/postgresql/data\n      - ./init.sql:/docker-entrypoint-initdb.d/init.sql  # Run SQL on first start\n    environment:\n      POSTGRES_USER: myuser\n      POSTGRES_PASSWORD: mypassword\n      POSTGRES_DB: myapp\n\n  redis:\n    image: redis:7-alpine\n    networks:\n      - backend-net\n    volumes:\n      - redis-data:/data\n    command: redis-server --appendonly yes  # Enable persistence\n\nnetworks:\n  frontend-net:\n  backend-net:\n\nvolumes:\n  db-data:\n  uploads:\n  redis-data:"
    },
    {
      "type": "text",
      "heading": "Key concepts in this file",
      "headingLevel": 3,
      "body": ""
    },
    {
      "type": "cardGrid",
      "columns": 2,
      "cards": [
        {
          "title": "services",
          "body": "Each service is one container. You give each one a name (frontend, backend, db) and tell Docker how to build or find it."
        },
        {
          "title": "build",
          "body": "Tells Docker to build an image from a Dockerfile. You specify the context (folder) and optionally which Dockerfile to use."
        },
        {
          "title": "image",
          "body": "Instead of building, you can use a pre-built image from Docker Hub. The \"postgres:16\" image is maintained by the PostgreSQL team."
        },
        {
          "title": "ports",
          "body": "Same as the -p flag in docker run. Maps a port on your computer to a port inside the container."
        },
        {
          "title": "volumes",
          "body": "Persistent storage. Named volumes (like db-data) survive container restarts. Without them, your database would lose all data when the container stops."
        },
        {
          "title": "depends_on",
          "body": "Controls startup order. With \"condition: service_healthy\", Docker waits until the backend passes its health check before starting the frontend."
        },
        {
          "title": "healthcheck",
          "body": "Defines how Docker checks if a service is working. It runs a test command on an interval and marks the service as healthy or unhealthy."
        },
        {
          "title": "environment",
          "body": "Sets environment variables inside the container. This is how you pass configuration (database passwords, API URLs) without hardcoding them."
        }
      ]
    },
    {
      "type": "alert",
      "severity": "info",
      "title": "Starting everything",
      "body": "Run `docker compose up` in the folder with your docker-compose.yml file. Docker will build all the images (if needed), create the containers, and start them together. Add `-d` to run in the background. Run `docker compose down` to stop everything."
    },
    {
      "type": "text",
      "heading": "Health Check Configuration",
      "headingLevel": 3,
      "body": "Health checks let Docker know whether your application is actually working, not just running. You can define them in the Dockerfile itself or in docker-compose.yml. When a container fails its health check, Docker marks it as unhealthy, and dependent services will not start."
    },
    {
      "type": "code",
      "language": "dockerfile",
      "filename": "Dockerfile (with health check)",
      "code": "FROM node:22-alpine\n\nWORKDIR /app\nCOPY package.json package-lock.json ./\nRUN npm ci --omit=dev\nCOPY . .\n\n# Install curl for the health check (Alpine does not include it)\nRUN apk add --no-cache curl\n\nEXPOSE 3000\n\n# Health check: hit the /health endpoint every 30 seconds\nHEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \\\n  CMD curl -f http://localhost:3000/health || exit 1\n\nCMD [\"node\", \"index.js\"]"
    },
    {
      "type": "text",
      "heading": "Development vs Production",
      "headingLevel": 2,
      "body": "Your Docker setup should behave differently depending on where it is running."
    },
    {
      "type": "text",
      "body": "During **development**, you want your code changes to appear immediately without rebuilding the container. During **production**, you want a clean, optimized build that is fast and secure. Docker Compose supports this with multiple configuration files."
    },
    {
      "type": "tabs",
      "tabs": [
        {
          "label": "Development",
          "blocks": [
            {
              "type": "code",
              "language": "yaml",
              "filename": "docker-compose.dev.yml",
              "code": "services:\n  frontend:\n    build:\n      context: ./frontend\n      dockerfile: Dockerfile.dev       # Use a dev-specific Dockerfile\n    ports:\n      - \"3000:3000\"\n    volumes:\n      - ./frontend/src:/app/src        # Mount source code — changes appear instantly\n      - /app/node_modules              # Do NOT mount node_modules (use container's copy)\n    environment:\n      - NODE_ENV=development\n      - WATCHPACK_POLLING=true         # Enable hot reload in Docker\n\n  backend:\n    build:\n      context: ./backend\n      dockerfile: Dockerfile.dev\n    ports:\n      - \"5000:5000\"\n    volumes:\n      - ./backend:/app                 # Mount all backend code\n      - /app/__pycache__               # Exclude Python cache\n    environment:\n      - FLASK_ENV=development\n      - FLASK_DEBUG=1                  # Auto-restart on code changes"
            },
            {
              "type": "alert",
              "severity": "info",
              "title": "The secret sauce",
              "body": "The volumes section mounts your local source code into the container. When you edit a file on your computer, the change is immediately visible inside the container. Combined with hot-reload tools, your app updates in real time without rebuilding."
            }
          ]
        },
        {
          "label": "Production",
          "blocks": [
            {
              "type": "code",
              "language": "yaml",
              "filename": "docker-compose.yml",
              "code": "services:\n  frontend:\n    build:\n      context: ./frontend\n      dockerfile: Dockerfile           # Uses the production multi-stage Dockerfile\n    ports:\n      - \"3000:3000\"\n    restart: always                    # Auto-restart if the container crashes\n    environment:\n      - NODE_ENV=production\n\n  backend:\n    build:\n      context: ./backend\n      dockerfile: Dockerfile\n    ports:\n      - \"5000:5000\"\n    restart: always\n    environment:\n      - FLASK_ENV=production\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:5000/health\"]\n      interval: 30s\n      timeout: 10s\n      retries: 3"
            },
            {
              "type": "alert",
              "severity": "warning",
              "title": "No volume mounts for code",
              "body": "In production, the code is baked into the image during the build step. There are no live code mounts. This ensures the running app is exactly what was tested and built."
            }
          ]
        }
      ]
    },
    {
      "type": "text",
      "heading": "Running each configuration",
      "headingLevel": 3,
      "body": ""
    },
    {
      "type": "code",
      "language": "bash",
      "code": "# Development — uses the dev compose file\ndocker compose -f docker-compose.dev.yml up\n\n# Production — uses the default compose file\ndocker compose up -d"
    },
    {
      "type": "text",
      "heading": "Debugging Containers",
      "headingLevel": 2,
      "body": "When something goes wrong inside a container, you need to get inside and poke around. The `docker exec` command lets you run commands inside a running container, just like opening a terminal on a remote server."
    },
    {
      "type": "code",
      "language": "bash",
      "description": "Common docker exec commands for debugging running containers.",
      "code": "# Open an interactive shell inside a running container\ndocker exec -it my-running-app sh\n\n# If the container has bash installed (not Alpine), use bash instead\ndocker exec -it my-running-app bash\n\n# Check environment variables inside the container\ndocker exec my-running-app env\n\n# View the filesystem -- useful to verify files were copied correctly\ndocker exec my-running-app ls -la /app\n\n# Check network connectivity from inside the container\ndocker exec my-running-app ping -c 3 db\n\n# Tail the last 100 lines of a log file inside the container\ndocker exec my-running-app tail -100 /app/logs/error.log\n\n# Run a database query from inside the backend container\ndocker exec my-running-app python -c \"from app import db; print(db.engine.url)\"\n\n# Check running processes inside the container\ndocker exec my-running-app ps aux\n\n# Inspect resource usage of all running containers\ndocker stats --no-stream"
    },
    {
      "type": "text",
      "heading": "Common Docker Commands",
      "headingLevel": 2,
      "body": "A quick reference for the commands you will use every day."
    },
    {
      "type": "table",
      "heading": "Docker Commands Cheat Sheet",
      "headers": [
        "Command",
        "What It Does",
        "Example"
      ],
      "rows": [
        [
          "docker build -t <name> .",
          "Build an image from the Dockerfile in the current directory",
          "docker build -t my-app ."
        ],
        [
          "docker run -p <host>:<container> <image>",
          "Create and start a container from an image with port mapping",
          "docker run -p 3000:3000 my-app"
        ],
        [
          "docker ps",
          "List running containers (add -a for all, including stopped)",
          "docker ps -a"
        ],
        [
          "docker logs <container>",
          "View stdout/stderr output (add -f to follow in real time)",
          "docker logs -f my-running-app"
        ],
        [
          "docker exec -it <container> <cmd>",
          "Run a command inside a running container interactively",
          "docker exec -it my-running-app bash"
        ],
        [
          "docker stop <container>",
          "Gracefully stop a running container",
          "docker stop my-running-app"
        ],
        [
          "docker rm <container>",
          "Remove a stopped container (add -f to force-remove a running one)",
          "docker rm my-running-app"
        ],
        [
          "docker image ls",
          "List all images on your machine with their sizes",
          "docker image ls"
        ],
        [
          "docker image rm <image>",
          "Remove an image from your machine",
          "docker image rm my-app"
        ],
        [
          "docker system prune",
          "Remove all stopped containers, unused networks, and dangling images",
          "docker system prune -a"
        ]
      ]
    },
    {
      "type": "text",
      "heading": "Docker Compose commands",
      "headingLevel": 3,
      "body": ""
    },
    {
      "type": "table",
      "heading": "Docker Compose Commands",
      "headers": [
        "Command",
        "Description",
        "Example"
      ],
      "rows": [
        [
          "docker compose up",
          "Build images (if needed) and start all services defined in docker-compose.yml. Add -d to run in the background. Add --build to force a rebuild.",
          "docker compose up -d --build"
        ],
        [
          "docker compose down",
          "Stop and remove all containers, networks, and resources created by 'up'. Add -v to also remove volumes (deletes data!).",
          "docker compose down -v"
        ],
        [
          "docker compose logs <service>",
          "View logs for a specific service. Omit the service name to see logs from all services.",
          "docker compose logs -f backend"
        ]
      ]
    },
    {
      "type": "text",
      "heading": "Troubleshooting",
      "headingLevel": 2,
      "body": "Docker problems can be confusing at first. Here are the most common issues and how to fix them."
    },
    {
      "type": "accordion",
      "title": "Port conflicts -- \"port is already allocated\"",
      "blocks": [
        {
          "type": "text",
          "body": "*Symptoms:* Error message: \"Bind for 0.0.0.0:3000 failed: port is already allocated\""
        },
        {
          "type": "list",
          "heading": "Solutions",
          "items": [
            "Something else is already using that port. Find it with: `lsof -i :3000` (Mac/Linux) or `netstat -ano | findstr :3000` (Windows).",
            "Stop the other process, or change the host port in your mapping: `docker run -p 3001:3000 my-app` -- this maps your port 3001 to the container's port 3000.",
            "If a previous Docker container is still running, stop it first: `docker stop <container-name>`.",
            "When using Docker Compose, run `docker compose down` before `docker compose up` to ensure old containers are cleaned up."
          ]
        }
      ]
    },
    {
      "type": "accordion",
      "title": "Build cache problems -- stale dependencies",
      "blocks": [
        {
          "type": "text",
          "body": "*Symptoms:* You updated package.json or requirements.txt but the container still uses old dependency versions. Or the build seems to skip steps that should run."
        },
        {
          "type": "list",
          "heading": "Solutions",
          "items": [
            "Force a full rebuild with no cache: `docker build --no-cache -t my-app .`",
            "With Docker Compose: `docker compose build --no-cache`",
            "If only dependencies changed, make sure your Dockerfile copies the lockfile before the source code so the cache invalidates correctly.",
            "To clear all build cache: `docker builder prune` (this frees disk space too).",
            "Remember that each Dockerfile instruction is a cache layer. If you change an early instruction, all subsequent layers are rebuilt."
          ]
        }
      ]
    },
    {
      "type": "accordion",
      "title": "Volume permission errors",
      "blocks": [
        {
          "type": "text",
          "body": "*Symptoms:* \"Permission denied\" errors when writing to mounted volumes, or files created inside the container are owned by root on the host."
        },
        {
          "type": "list",
          "heading": "Solutions",
          "items": [
            "On Linux, the container user's UID must match the host user's UID. Add `user: \"1000:1000\"` to your docker-compose.yml service to match your host user.",
            "In the Dockerfile, create a user with the same UID as your host user: `RUN adduser -u 1000 -D appuser`.",
            "For named volumes, Docker manages permissions automatically. Problems mostly occur with bind mounts (host directory mounts).",
            "As a quick fix, you can run `chmod -R 777 ./data` on the host directory, but this is not recommended for production.",
            "On macOS and Windows, Docker Desktop handles permission mapping automatically, so this is mainly a Linux issue."
          ]
        }
      ]
    },
    {
      "type": "accordion",
      "title": "Container won't start or exits immediately",
      "blocks": [
        {
          "type": "text",
          "body": "*Symptoms:* You run docker run and the container exits immediately with a status code, or you see an error message."
        },
        {
          "type": "list",
          "heading": "Solutions",
          "items": [
            "Check the logs: `docker logs <container-name>` -- this almost always reveals the error.",
            "Make sure your CMD in the Dockerfile is correct. A typo in the filename (e.g., \"app.py\" vs \"main.py\") will crash the container.",
            "Verify that all required environment variables are set. Missing config is a common cause of immediate exits.",
            "If the container runs and exits instantly, your app might not have a long-running process. A web server stays alive, but a script that finishes will cause the container to stop.",
            "Try running the container interactively to debug: `docker run -it my-app sh`"
          ]
        }
      ]
    },
    {
      "type": "accordion",
      "title": "Image too large -- slow builds and deploys",
      "blocks": [
        {
          "type": "text",
          "body": "*Symptoms:* Your Docker image is several gigabytes, builds take forever, and pushing to a registry is painfully slow."
        },
        {
          "type": "list",
          "heading": "Solutions",
          "items": [
            "Use Alpine-based images (`node:22-alpine` instead of `node:22`) -- they are 5-10x smaller.",
            "Use multi-stage builds to separate the build environment from the runtime. Your final image only needs the compiled output, not the build tools.",
            "Create a .dockerignore file to exclude node_modules, .git, and other large directories from the build context.",
            "Combine RUN commands to reduce the number of layers: `RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*`",
            "Check image size with `docker image ls` and inspect layers with `docker history <image>` to find what is taking up space.",
            "Remove unnecessary files in the same RUN command that creates them: `RUN apt-get install -y build-essential && make && apt-get purge -y build-essential`"
          ]
        }
      ]
    },
    {
      "type": "alert",
      "severity": "success",
      "title": "Golden rule of Docker debugging",
      "body": "Always check the logs first. Run `docker logs <container-name>` or `docker compose logs <service-name>`. Nine times out of ten, the answer is right there in the output."
    },
    {
      "type": "text",
      "heading": "Multi-Platform Builds",
      "headingLevel": 2,
      "body": "By default, Docker builds images only for the CPU architecture of the machine you are building on (usually `amd64`). But if you deploy to ARM servers (AWS Graviton, Raspberry Pi) or your teammates use Apple Silicon Macs, you need images that work on both `amd64` and `arm64`. Docker Buildx with QEMU makes this possible from a single machine."
    },
    {
      "type": "code",
      "language": "bash",
      "description": "Building a multi-platform image locally using Docker Buildx and QEMU.",
      "code": "# 1. Install QEMU emulation support (one-time setup)\ndocker run --rm --privileged multiarch/qemu-user-static --reset -p yes\n\n# 2. Create a Buildx builder that supports multi-platform\ndocker buildx create --name multiplatform --use\ndocker buildx inspect --bootstrap\n\n# 3. Build and push a multi-platform image\ndocker buildx build \\\n  --platform linux/amd64,linux/arm64 \\\n  --tag ghcr.io/youruser/myapp-backend:latest \\\n  --push \\\n  ./backend\n\n# 4. Verify the manifest lists both architectures\ndocker manifest inspect ghcr.io/youruser/myapp-backend:latest"
    },
    {
      "type": "alert",
      "severity": "info",
      "title": "Multi-platform images use manifests",
      "body": "A multi-platform image is not two separate images -- it is a single image reference (tag) backed by a **manifest list**. When you `docker pull`, Docker automatically selects the right architecture for your machine. You push once, and it works everywhere."
    },
    {
      "type": "text",
      "body": "In CI/CD pipelines (like GitHub Actions), you typically use `docker/setup-qemu-action` and `docker/setup-buildx-action` to handle the setup automatically. See the **CI/CD with GitHub Actions** section for a complete workflow example."
    },
    {
      "type": "text",
      "heading": "You Know Docker!",
      "headingLevel": 2,
      "body": "You now understand what Docker is, how to write a Dockerfile, build and run containers, orchestrate multi-container setups with Docker Compose, configure separate development and production environments, and build multi-platform images for amd64 and arm64."
    },
    {
      "type": "text",
      "body": "**Next steps:** Try dockerizing one of your own projects. Start with a simple Dockerfile, then add Docker Compose when you need multiple services. The best way to learn Docker is to use it on a real project."
    }
  ],
  "keyPoints": [
    "Docker packages your app and all its dependencies into a container that runs the same everywhere",
    "Containers are lightweight and start in seconds, unlike virtual machines",
    "A Dockerfile is a recipe for building an image; docker build and docker run are the two essential commands",
    "Docker Compose lets you define and run multi-container setups with a single YAML file",
    "Use volume mounts in development for live code reloading, and baked-in images for production",
    "Multi-platform builds with Buildx and QEMU let you produce images for both amd64 and arm64 from a single machine",
    "Always check container logs first when troubleshooting issues"
  ],
  "references": [
    {
      "id": "docker2024",
      "authors": "Docker Inc.",
      "year": "2024",
      "title": "Docker Documentation",
      "source": "Docker Docs",
      "url": "https://docs.docker.com/"
    },
    {
      "id": "merkel2014",
      "authors": "Merkel, D.",
      "year": "2014",
      "title": "Docker: Lightweight Linux Containers for Consistent Development and Deployment",
      "source": "Linux Journal, 2014(239), p.2"
    },
    {
      "id": "turnbull2014",
      "authors": "Turnbull, J.",
      "year": "2014",
      "title": "The Docker Book",
      "source": "James Turnbull"
    }
  ]
}